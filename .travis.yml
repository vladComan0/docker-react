sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t vladcoman/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true vladcoman/docker-react npm run test
